# cam_lidar 스파이크 결과

- open3d: 0.18.0 설치됨 (aarch64 휠 정상, `pip install open3d`)
- 3D 픽킹: VisualizerWithEditing 클래스 가용(import·클래스 확인 완료). 단 이 호스트는
  헤드리스(DISPLAY 미설정)라 실제 픽킹 창은 디스플레이/X-forwarding 붙은 실기에서 확인 필요.
